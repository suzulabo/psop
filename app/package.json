{
  "name": "psop-app",
  "private": true,
  "scripts": {
    "build": "rm -rf www && stencil build",
    "start": "stencil build --dev --watch --serve",
    "start.sw": "stencil build --dev --watch --serve --service-worker",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watch",
    "generate": "stencil generate"
  },
  "devDependencies": {
    "@rollup/plugin-replace": "^2.3.4",
    "@stencil/core": "^2.4.0",
    "@stencil/sass": "^1.4.1",
    "base32-decode": "^1.0.0",
    "base32-encode": "^1.1.1",
    "bootstrap-icons": "^1.3.0",
    "clipboard-polyfill": "^3.0.2",
    "destyle.css": "^2.0.2",
    "stencil-router-v2": "^0.6.0",
    "tweetnacl": "^1.0.3",
    "workbox-build": "^4.3.1"
  }
}
